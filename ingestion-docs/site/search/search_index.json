{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Sorsa Ingestion \u2014 Documentation","text":"<p>This documentation covers the full implementation of the Sorsa ingestion pipeline located under <code>app/</code> in the TestSorsa repository. Every section is derived directly from the source code and DDL rather than from aspirational plans.</p>"},{"location":"#what-is-this","title":"What is this?","text":"<p>The Sorsa ingestion pipeline is a Python 3.12 async application that:</p> <ol> <li>Pulls X/Twitter intelligence data from the Sorsa v3 REST API.</li> <li>Stores raw and normalized data into CockroachDB.</li> <li>Tracks each ingestion run and every paginated request window in checkpoint tables.</li> <li>Performs idempotent upserts so re-runs never produce duplicates.</li> </ol> <p>It is designed to run locally via the CLI today and can be ported to any worker framework without changing business logic \u2014 all logic lives in <code>IngestionOrchestrator.run_project_ingestion(...)</code>.</p>"},{"location":"#repository-layout","title":"Repository layout","text":"<pre><code>app/\n\u251c\u2500\u2500 __init__.py\n\u251c\u2500\u2500 cli.py                        # CLI entrypoint \u2014 parses args, loads .env, calls orchestrator\n\u251c\u2500\u2500 config.py                     # Pydantic-settings: all runtime config from env vars\n\u251c\u2500\u2500 models.py                     # Shared dataclasses: TimeSlice, IngestionContext\n\u251c\u2500\u2500 clients/\n\u2502   \u2514\u2500\u2500 sorsa_client.py           # Async HTTP client \u2014 4 endpoint wrappers + rate limiter + retry\n\u251c\u2500\u2500 db/\n\u2502   \u251c\u2500\u2500 connection.py             # SQLAlchemy async engine + session maker factory\n\u2502   \u2514\u2500\u2500 repository.py            # All DB reads/writes: runs, raw posts, normalized posts, scores, checkpoints\n\u2514\u2500\u2500 pipeline/\n    \u251c\u2500\u2500 orchestrator.py           # Top-level coordinator \u2014 wires all components, controls phase order\n    \u251c\u2500\u2500 search_ingestor.py        # Phase 1: concurrent 72-hour time-sliced search ingestion\n    \u2514\u2500\u2500 aux_ingestors.py          # Phases 2\u20134: comments, user timelines, user scores\n\nmain.py                           # Python entry: delegates to app.cli.main()\nddl/\n\u2514\u2500\u2500 ddl_mindshare_ingestion.sql   # Schema DDL for all ingestion tables\nMindshare DDL/\n\u2514\u2500\u2500 ddl mindshare.sql             # Original DDL \u2014 not modified\n</code></pre>"},{"location":"#implemented-api-endpoints","title":"Implemented API endpoints","text":"Endpoint Method Purpose <code>/search-tweets</code> POST Search tweets by keyword + time window <code>/comments</code> POST Fetch all comments on a given post <code>/user-tweets</code> POST Fetch a user's recent tweet timeline <code>/score-id/{x-id}</code> GET Fetch influence score + profile for a user"},{"location":"#implemented-features","title":"Implemented features","text":"<ul> <li>72-hour sliding window search with configurable time-slicing for parallel fetch.</li> <li>Per-key RPS limiter \u2014 enforces the flat <code>SORSA_PER_KEY_RPS</code> requests-per-second cap. Thread-safe, asyncio-native, shared across all concurrent slice workers.</li> <li>Retry handling \u2014 automatic backoff and retry for HTTP 429, HTTP 5xx, and network/timeout errors.</li> <li>Dual-layer persistence \u2014 every API response is stored both as raw JSONB (for debugging/replay) and as a normalized upsert (for production queries).</li> <li><code>project_keywords</code> merge \u2014 a post seen by multiple keyword searches accumulates all keywords in a <code>TEXT[]</code> array rather than clobbering the earlier write.</li> <li>Ingestion run lifecycle \u2014 every pipeline execution creates an <code>ingestion_run</code> record that is transitioned from <code>running</code> \u2192 <code>completed</code> or <code>running</code> \u2192 <code>failed</code>.</li> <li>Window checkpoints \u2014 every paginated request window records its cursor and status in <code>ingestion_window_checkpoint</code>, enabling future resume logic.</li> </ul>"},{"location":"#database-schemas","title":"Database schemas","text":"<p>Two CockroachDB schemas are used:</p> <ul> <li><code>mindshare</code> \u2014 normalized production data: posts, users, runs, checkpoints.</li> <li><code>raw_data</code> \u2014 raw JSONB ingestion payloads for debugging and replay.</li> </ul> <p>See Data Model for full table documentation.</p>"},{"location":"#quick-navigation","title":"Quick navigation","text":"Topic Page Setup and running Quickstart System design and module responsibilities Architecture Table schemas and upsert semantics Data Model Phase-by-phase pipeline logic Pipeline Behavior SQL checks, tuning, known limitations Operations Runbook"},{"location":"architecture/","title":"Architecture","text":"<p>This page documents every module in <code>app/</code>, its responsibilities, its internal design decisions, and how it connects to the rest of the system.</p>"},{"location":"architecture/#system-overview","title":"System overview","text":"<pre><code>flowchart TD\n    entry[\"main.py\\ncli.main()\"]\n    config[\"app/config.py\\nSettings\"]\n    orch[\"app/pipeline/orchestrator.py\\nIngestionOrchestrator\"]\n    search[\"app/pipeline/search_ingestor.py\\nSearchIngestor\"]\n    aux[\"app/pipeline/aux_ingestors.py\\nAuxiliaryIngestors\"]\n    client[\"app/clients/sorsa_client.py\\nSorsaClient + PerKeyRateLimiter\"]\n    repo[\"app/db/repository.py\\nIngestionRepository\"]\n    conn[\"app/db/connection.py\\nbuild_engine / build_session_maker\"]\n    db[(\"CockroachDB\\nmindshare + raw_data schemas\")]\n    sorsa_api[\"Sorsa v3 API\\nhttps://api.sorsa.io/v3\"]\n\n    entry --&gt; config\n    entry --&gt; orch\n    orch --&gt; search\n    orch --&gt; aux\n    orch --&gt; repo\n    search --&gt; client\n    search --&gt; repo\n    aux --&gt; client\n    aux --&gt; repo\n    repo --&gt; conn\n    conn --&gt; db\n    client --&gt; sorsa_api</code></pre>"},{"location":"architecture/#module-by-module-breakdown","title":"Module-by-module breakdown","text":""},{"location":"architecture/#mainpy","title":"<code>main.py</code>","text":"<p>The Python entry point. Contains only:</p> <pre><code>from app.cli import main\nif __name__ == \"__main__\":\n    main()\n</code></pre> <p>No business logic. Exists purely to allow <code>python main.py</code> invocation from the repo root.</p>"},{"location":"architecture/#appclipy-cli-entrypoint","title":"<code>app/cli.py</code> \u2014 CLI entrypoint","text":"<p>Responsibility: parse CLI arguments, bootstrap the runtime environment, drive the async orchestrator.</p> <p>How it works:</p> <ol> <li>Defines <code>main()</code> which creates an <code>argparse.ArgumentParser</code> with a single required argument: <code>--project-keyword</code>.</li> <li>Calls <code>asyncio.run(_run(keyword))</code> to enter the async event loop.</li> <li>Inside <code>_run</code>, calls <code>load_dotenv()</code> first \u2014 this reads <code>.env</code> from the working directory before <code>Settings</code> instantiation.</li> <li>Instantiates <code>Settings()</code> which binds all env vars via Pydantic-settings.</li> <li>Constructs <code>IngestionOrchestrator(settings)</code>.</li> <li>Awaits <code>orchestrator.run_project_ingestion(project_keyword)</code> \u2014 this is the entire pipeline.</li> <li>In a <code>finally</code> block, always calls <code>orchestrator.close()</code> to dispose the SQLAlchemy engine and release DB connections.</li> </ol> <p>Worker portability: A background worker can skip the CLI layer entirely and call <code>IngestionOrchestrator.run_project_ingestion(keyword)</code> directly after constructing an orchestrator with a <code>Settings</code> instance. No CLI assumptions leak into the pipeline.</p>"},{"location":"architecture/#appconfigpy-settings","title":"<code>app/config.py</code> \u2014 Settings","text":"<p>Responsibility: central, validated, typed configuration loaded from environment variables.</p> <p>Implementation: extends <code>pydantic_settings.BaseSettings</code> with <code>SettingsConfigDict</code>: - <code>env_file = \".env\"</code> \u2014 reads <code>.env</code> automatically (but <code>cli.py</code> calls <code>load_dotenv()</code> first for explicit control). - <code>env_file_encoding = \"utf-8\"</code> - <code>extra = \"ignore\"</code> \u2014 silently ignores unknown env vars.</p> <p>Fields:</p> Field Env var Default Type <code>cockroach_database_url</code> <code>COCKROACH_DATABASE_URL</code> required <code>str</code> <code>sorsa_base_url</code> <code>SORSA_BASE_URL</code> <code>https://api.sorsa.io/v3</code> <code>str</code> <code>sorsa_api_key</code> <code>SORSA_API_KEY</code> required <code>str</code> <code>sorsa_per_key_rps</code> <code>SORSA_PER_KEY_RPS</code> <code>20</code> <code>int</code> <code>search_slice_count</code> <code>SEARCH_SLICE_COUNT</code> <code>20</code> <code>int</code> <code>search_max_concurrency</code> <code>SEARCH_MAX_CONCURRENCY</code> <code>20</code> <code>int</code> <code>search_order</code> <code>SEARCH_ORDER</code> <code>\"latest\"</code> <code>str</code> <code>max_retries</code> <code>SORSA_MAX_RETRIES</code> <code>4</code> <code>int</code> <code>retry_429_sleep_seconds</code> <code>SORSA_RETRY_429_SLEEP_SECONDS</code> <code>1.0</code> <code>float</code> <code>retry_5xx_sleep_seconds</code> <code>SORSA_RETRY_5XX_SLEEP_SECONDS</code> <code>2.0</code> <code>float</code> <p><code>api_keys</code> property: returns <code>[self.sorsa_api_key.strip()]</code> \u2014 a single-element list. The orchestrator iterates this list using round-robin index assignment (<code>key_idx % len(keys)</code>), so the design is already multi-key capable; the config just doesn't expose comma-separated parsing yet.</p>"},{"location":"architecture/#appmodelspy-shared-dataclasses","title":"<code>app/models.py</code> \u2014 Shared dataclasses","text":"<p><code>TimeSlice</code> \u2014 frozen dataclass representing one time-window segment assigned to an API key:</p> Field Type Description <code>slice_id</code> <code>int</code> 1-based index <code>since</code> <code>datetime</code> Start of this slice (UTC) <code>until</code> <code>datetime</code> End of this slice (UTC) <code>api_key_alias</code> <code>str</code> Which key alias is assigned to this slice <p>Used by <code>build_time_slices()</code> in <code>search_ingestor.py</code> and consumed by <code>SearchIngestor._ingest_slice()</code>.</p> <p><code>IngestionContext</code> \u2014 mutable dataclass holding run metadata (run_id, project_keyword, slice_id, endpoint, api_key_alias). Currently unused in running code \u2014 reserved for future structured context propagation.</p> <p><code>JsonDict</code> \u2014 type alias for <code>dict[str, Any]</code>.</p>"},{"location":"architecture/#appclientssorsa_clientpy-http-client","title":"<code>app/clients/sorsa_client.py</code> \u2014 HTTP client","text":"<p>Responsibility: all communication with the Sorsa v3 REST API \u2014 rate limiting, retrying, authentication.</p>"},{"location":"architecture/#perkeyratelimiter","title":"<code>PerKeyRateLimiter</code>","text":"<p>Enforces a maximum of <code>rps</code> requests per second per API key alias. The Sorsa API allows a flat <code>SORSA_PER_KEY_RPS</code> requests every second (default: 20).</p> <p>Implementation: - Maintains a <code>deque[float]</code> of event timestamps for each alias, keyed by <code>alias</code> string. - <code>acquire(alias)</code> is an async method that:   1. Locks (<code>asyncio.Lock</code>) and trims timestamps older than 1 second from the front of the deque.   2. If the deque length is below <code>rps</code>, appends the current monotonic time and returns \u2014 the request may proceed.   3. Otherwise calculates <code>sleep_for = 1.0 - (now - q[0])</code>, releases the lock, and sleeps until the oldest tracked request falls outside the 1-second window.   4. Loops until a slot is available. - The lock is released before sleeping so other coroutines are not blocked while waiting. - <code>rps</code> is floored to 1 via <code>max(1, rps)</code>.</p>"},{"location":"architecture/#apikeystate","title":"<code>ApiKeyState</code>","text":"<p>A simple <code>@dataclass</code> holding <code>alias: str</code> and <code>api_key: str</code>. The alias is used for rate limiter keying and checkpoint labeling.</p>"},{"location":"architecture/#sorsaclient","title":"<code>SorsaClient</code>","text":"<p>Stateless HTTP call wrapper. Constructor receives the rate limiter (injected), base URL, timeout, and retry config.</p> <p><code>_request(method, path, key_state, payload)</code> \u2014 core request method:</p> <ol> <li>Calls <code>self._limiter.acquire(key_state.alias)</code> before every attempt (including retries).</li> <li>Opens a fresh <code>aiohttp.ClientSession</code> per request (not per client instance). This avoids connection reuse issues across concurrent coroutines.</li> <li>Sends <code>ApiKey: &lt;key&gt;</code> and <code>Accept: application/json</code> headers; adds <code>Content-Type: application/json</code> for POST.</li> <li>Parses response JSON with <code>content_type=None</code> to tolerate non-standard content-type headers.</li> <li>On success (2xx): returns the response dict (or <code>{}</code> if the body isn't a dict).</li> <li>On HTTP 429: sleeps <code>retry_429_sleep_seconds</code> and retries if attempts remain; raises <code>SorsaRateLimitError</code> on final attempt.</li> <li>On HTTP 5xx: sleeps <code>retry_5xx_sleep_seconds</code> and retries; raises <code>SorsaClientError</code> on final attempt.</li> <li>On <code>aiohttp.ClientError</code> or <code>asyncio.TimeoutError</code>: sleeps <code>retry_5xx_sleep_seconds</code> and retries; raises <code>SorsaClientError</code> on final attempt.</li> <li>Falls through to a terminal <code>raise SorsaClientError(\"request retries exhausted\")</code> as a safety net.</li> </ol> <p>Endpoint methods:</p> Method HTTP Path Key params <code>search_tweets</code> POST <code>/search-tweets</code> <code>query</code> (includes <code>since:</code> / <code>until:</code> inline), <code>order</code>, optional <code>next_cursor</code> <code>comments</code> POST <code>/comments</code> <code>tweet_link</code> (post ID as string), optional <code>next_cursor</code> <code>user_tweets</code> POST <code>/user-tweets</code> <code>user_id</code>, optional <code>next_cursor</code> <code>score_id</code> GET <code>/score-id/{x_id}</code> path param only <p><code>to_sorsa_date(dt)</code> \u2014 utility function that formats a <code>datetime</code> into Sorsa's expected format: <code>YYYY-MM-DD_HH:MM:SS_UTC</code>. Called by <code>SearchIngestor._ingest_slice()</code> when constructing <code>since:</code> / <code>until:</code> query clauses.</p>"},{"location":"architecture/#appdbconnectionpy-engine-factory","title":"<code>app/db/connection.py</code> \u2014 Engine factory","text":"<p>Responsibility: create the SQLAlchemy async engine and session factory.</p> <p><code>build_engine(settings)</code>: - Calls <code>create_async_engine</code> with <code>pool_pre_ping=True</code>, <code>pool_size=20</code>, <code>max_overflow=10</code>. - <code>pool_pre_ping</code> ensures stale connections are detected and recycled before use.</p> <p><code>build_session_maker(engine)</code>: - Returns an <code>async_sessionmaker[AsyncSession]</code> with <code>expire_on_commit=False</code>. - <code>expire_on_commit=False</code> prevents SQLAlchemy from expiring ORM objects after commit (important for async patterns where lazy loading doesn't work).</p> <p>Both are called once in <code>IngestionOrchestrator.__init__</code> and the session maker is shared across all repository calls for the lifetime of one run.</p>"},{"location":"architecture/#appdbrepositorypy-data-access-layer","title":"<code>app/db/repository.py</code> \u2014 Data access layer","text":"<p>Responsibility: all SQL. No business logic; takes validated Python values and executes the correct SQL statement.</p> <p>Every method opens a fresh <code>AsyncSession</code> using <code>async with self._session_maker() as session</code>, executes, commits, and closes. The session scope is per-operation (not per run).</p>"},{"location":"architecture/#create_runproject_keyword-since-until-str","title":"<code>create_run(project_keyword, since, until) -&gt; str</code>","text":"<p>Generates a UUID (<code>str(uuid4())</code>), inserts a row into <code>mindshare.ingestion_run</code> with <code>run_status = 'running'</code> and returns the UUID. Called at the start of every <code>run_project_ingestion</code>.</p>"},{"location":"architecture/#mark_run_finishedrun_id-status-errornone","title":"<code>mark_run_finished(run_id, status, error=None)</code>","text":"<p>Updates <code>mindshare.ingestion_run</code> setting <code>run_status</code>, <code>error_summary</code>, and <code>finished_at = now()</code>. Called with <code>'completed'</code> on success or <code>'failed'</code> with the exception string on any uncaught error.</p>"},{"location":"architecture/#upsert_post_rawrun_id-project_keyword-endpoint-payload","title":"<code>upsert_post_raw(run_id, project_keyword, endpoint, payload)</code>","text":"<p>Inserts one row into <code>raw_data.raw_post_ingestion</code>. The conflict key is <code>(post_id, endpoint)</code>. On conflict, replaces <code>raw_json</code>, <code>fetched_at</code>, <code>project_keyword</code>, and <code>run_id</code>. Skips silently if <code>payload[\"id\"]</code> is absent or empty.</p> <p>This table is a raw audit log \u2014 it preserves the last seen API payload for any post+endpoint combination, regardless of which project or run fetched it.</p>"},{"location":"architecture/#upsert_mindshare_postpayload-project_keyword-run_id","title":"<code>upsert_mindshare_post(payload, project_keyword, run_id)</code>","text":"<p>The most complex method. Normalizes a raw tweet dict into <code>mindshare.mindshare_post</code>.</p> <p>Field extraction from the raw payload:</p> DB column Source in payload <code>post_id</code> <code>payload[\"id\"]</code> <code>user_x_id</code> <code>payload[\"user\"][\"id\"]</code> <code>project_keywords</code> <code>ARRAY[project_keyword]</code> (merged on conflict \u2014 see below) <code>full_text</code> <code>payload[\"full_text\"]</code> <code>retweeted_post_id</code> <code>payload[\"retweeted_status\"][\"id\"]</code> <code>replied_post_id</code> <code>payload[\"in_reply_to_tweet_id\"]</code> <code>quoted_post_id</code> <code>payload[\"quoted_status\"][\"id\"]</code> <code>root_post_id</code> <code>payload[\"conversation_id_str\"]</code> <code>view_count</code> <code>payload[\"view_count\"]</code> <code>reply_count</code> <code>payload[\"reply_count\"]</code> <code>retweet_count</code> <code>payload[\"retweet_count\"]</code> <code>quote_count</code> <code>payload[\"quote_count\"]</code> <code>favorite_count</code> <code>payload[\"likes_count\"]</code> <code>entities</code> <code>payload[\"entities\"]</code> (stored as JSONB) <code>post_created_at</code> <code>payload[\"created_at\"]</code> <p>Skips rows where <code>post_id</code>, <code>user_x_id</code>, or <code>created_at</code> are missing/empty.</p> <p>Conflict resolution on <code>post_id</code>: - <code>project_keywords</code>: merged via <code>array_agg(DISTINCT k)</code> over <code>existing || incoming</code> \u2014 deduplicates, never loses a keyword. - <code>full_text</code>: only replaced if the incoming value is non-empty; preserves existing text otherwise. - All engagement counts: always overwritten with latest values. - <code>retweeted_post_id</code>, <code>replied_post_id</code>, <code>quoted_post_id</code>, <code>root_post_id</code>, <code>entities</code>: use <code>COALESCE(EXCLUDED, existing)</code> \u2014 keeps the first non-null value. - <code>last_seen_at</code>, <code>last_ingested_run_id</code>: always updated to reflect this run.</p>"},{"location":"architecture/#upsert_window_checkpointrun_id-project_keyword-window_id-endpoint-api_key_alias-next_cursor-status-error_messagenone","title":"<code>upsert_window_checkpoint(run_id, project_keyword, window_id, endpoint, api_key_alias, next_cursor, status, error_message=None)</code>","text":"<p>Upserts into <code>mindshare.ingestion_window_checkpoint</code>. Conflict key is <code>(run_id, project_keyword, window_id, endpoint)</code>. On conflict, updates <code>api_key_alias</code>, <code>next_cursor</code>, <code>status</code>, <code>error_message</code>, and <code>updated_at</code>. This is called after every page fetch to maintain a recoverable cursor.</p>"},{"location":"architecture/#upsert_user_scorex_id-username-display_name-avatar_url-followers_count-score","title":"<code>upsert_user_score(x_id, username, display_name, avatar_url, followers_count, score)</code>","text":"<p>Upserts into <code>mindshare.mindshare_user</code>. Conflict key is <code>x_id</code>. On conflict, updates <code>x_username</code>, <code>display_name</code>, <code>score</code>, <code>avatar_url</code>, <code>followers_count</code>, <code>last_score_fetched_at</code>, and <code>updated_at</code> \u2014 using <code>COALESCE(NULLIF(EXCLUDED.field, ''), existing)</code> so empty strings don't clobber non-empty stored values.</p>"},{"location":"architecture/#apppipelinesearch_ingestorpy-phase-1","title":"<code>app/pipeline/search_ingestor.py</code> \u2014 Phase 1","text":"<p>Responsibility: ingest all tweets matching the keyword from the last 72 hours.</p>"},{"location":"architecture/#build_time_slicessince-until-slice_count-keys-listtimeslice","title":"<code>build_time_slices(since, until, slice_count, keys) -&gt; list[TimeSlice]</code>","text":"<p>Module-level function. Divides <code>[since, until]</code> into <code>slice_count</code> equal sub-windows. Each slice is assigned an API key alias by round-robin: <code>keys[i % len(keys)].alias</code>. The last slice always ends exactly at <code>until</code> to avoid floating-point drift.</p> <p>Example with <code>slice_count=3</code> over 72 hours and one key:</p> <pre><code>slice_1: t+0h   \u2192 t+24h  (key_1)\nslice_2: t+24h  \u2192 t+48h  (key_1)\nslice_3: t+48h  \u2192 t+72h  (key_1)\n</code></pre>"},{"location":"architecture/#searchingestoringest_72h-tuplesetstr-setstr","title":"<code>SearchIngestor.ingest_72h(...) -&gt; tuple[set[str], set[str]]</code>","text":"<p>Returns <code>(all_post_ids, all_user_ids)</code> \u2014 the union of every post ID and user ID seen across all slices.</p> <ol> <li>Computes <code>until = now()</code>, <code>since = until - 72h</code>.</li> <li>Calls <code>build_time_slices()</code> to get the slice list.</li> <li>Builds a <code>key_map: {alias -&gt; ApiKeyState}</code> for O(1) lookup during slice execution.</li> <li>Computes <code>effective_concurrency = max(1, min(slice_count, max_concurrency, per_key_rps))</code>.</li> <li>Creates <code>asyncio.Semaphore(effective_concurrency)</code>.</li> <li>Launches all slice coroutines as tasks via <code>asyncio.gather(*tasks)</code>. Each coroutine acquires the semaphore before calling <code>_ingest_slice</code>.</li> <li>Merges results across all slices with <code>set.update()</code>.</li> </ol>"},{"location":"architecture/#searchingestor_ingest_slice-tuplesetstr-setstr","title":"<code>SearchIngestor._ingest_slice(...) -&gt; tuple[set[str], set[str]]</code>","text":"<p>Runs the paginated fetch loop for one time slice:</p> <ol> <li>Sets <code>window_id = f\"slice_{slice_.slice_id}\"</code>.</li> <li>Loops: calls <code>client.search_tweets()</code> with formatted since/until timestamps.</li> <li>For each tweet in <code>data[\"tweets\"]</code>, extracts <code>post_id</code> and <code>user_id</code>, adds to local sets, and calls <code>repo.upsert_post_raw()</code> and <code>repo.upsert_mindshare_post()</code>.</li> <li>Reads <code>data.get(\"next_cursor\")</code>. If present: calls <code>repo.upsert_window_checkpoint(status=\"running\")</code> and loops. If absent: calls <code>repo.upsert_window_checkpoint(status=\"completed\")</code> and returns.</li> <li>On any exception: calls <code>repo.upsert_window_checkpoint(status=\"failed\", error_message=str(exc))</code> and re-raises. The semaphore is released when the coroutine exits, allowing another slice to start.</li> </ol>"},{"location":"architecture/#apppipelineaux_ingestorspy-phases-24","title":"<code>app/pipeline/aux_ingestors.py</code> \u2014 Phases 2\u20134","text":"<p>Responsibility: sequential ingestion of comments, user timelines, and user scores for all IDs discovered in Phase 1.</p> <p>All three methods receive <code>run_id</code>, <code>project_keyword</code>, and <code>key: ApiKeyState</code> \u2014 they use only the first available key (passed by the orchestrator).</p>"},{"location":"architecture/#ingest_comments_for_postsposts-key","title":"<code>ingest_comments_for_posts(posts, key, ...)</code>","text":"<p>For each <code>post_id</code> in <code>posts</code>: - <code>window_id = f\"comments_{post_id}\"</code> - Paginated loop: calls <code>client.comments(tweet_link=post_id, cursor=cursor)</code>. - Each returned tweet is upserted raw and normalized (same calls as the search phase). - Checkpoint written with <code>status=\"running\"</code> (cursor present) or <code>\"completed\"</code> (no cursor). - On exception: checkpoint written with <code>status=\"failed\"</code> and <code>error_message</code>; loop breaks (<code>continue</code> to next post rather than propagating). This means a single failed post doesn't abort the whole comments phase.</p>"},{"location":"architecture/#ingest_user_tweetsuser_ids-key","title":"<code>ingest_user_tweets(user_ids, key, ...)</code>","text":"<p>Same pattern as comments but for user timelines: - <code>window_id = f\"user_{user_id}\"</code> - Calls <code>client.user_tweets(user_id=user_id, cursor=cursor)</code>. - Same raw + normalized upsert + checkpoint pattern. - Per-user errors are silently caught and checkpointed as <code>failed</code>; iteration continues.</p>"},{"location":"architecture/#ingest_scoresuser_ids-key","title":"<code>ingest_scores(user_ids, key, ...)</code>","text":"<p>Scores are not paginated \u2014 one GET per user: - <code>window_id = f\"score_{user_id}\"</code> - Calls <code>client.score_id(x_id=user_id)</code>. - Calls <code>repo.upsert_user_score()</code> with <code>username</code>, <code>display_name</code>, <code>profile_image_url</code>, <code>followers_count</code>, <code>score</code> extracted from the response. - Writes checkpoint with <code>status=\"completed\"</code> or <code>\"failed\"</code>. - Per-user errors are caught (not re-raised) \u2014 other users are still scored.</p>"},{"location":"architecture/#apppipelineorchestratorpy-top-level-coordinator","title":"<code>app/pipeline/orchestrator.py</code> \u2014 Top-level coordinator","text":"<p>Responsibility: wire all components together, control phase order, own the run lifecycle.</p> <p><code>__init__(settings)</code>: - Creates engine and session maker via <code>connection.py</code>. - Creates <code>IngestionRepository</code>. - Creates <code>PerKeyRateLimiter(settings.sorsa_per_key_rps)</code>. - Creates <code>SorsaClient</code> with all retry/timeout/limiter config. - Creates <code>SearchIngestor(repo, client)</code> and <code>AuxiliaryIngestors(repo, client)</code>.</p> <p><code>_api_keys()</code>: Converts <code>settings.api_keys</code> (a list of key strings) into <code>list[ApiKeyState]</code> with auto-generated aliases <code>key_1</code>, <code>key_2</code>, etc.</p> <p><code>run_project_ingestion(project_keyword) -&gt; str</code>:</p> <pre><code>now = datetime.now(UTC)\nsince = now - 72h\nrun_id = repo.create_run(...)\ntry:\n    post_ids, user_ids = search_ingestor.ingest_72h(...)\n    first_key = api_keys()[0]\n    aux_ingestors.ingest_comments_for_posts(posts=post_ids, key=first_key, ...)\n    aux_ingestors.ingest_user_tweets(user_ids=user_ids, key=first_key, ...)\n    aux_ingestors.ingest_scores(user_ids=user_ids, key=first_key, ...)\n    repo.mark_run_finished(run_id, \"completed\")\n    return run_id\nexcept Exception as exc:\n    repo.mark_run_finished(run_id, \"failed\", str(exc))\n    raise\n</code></pre> <p>Any uncaught exception from <code>SearchIngestor</code> (which re-raises slice failures) will abort aux phases, mark the run <code>failed</code>, and propagate to the CLI which prints the traceback.</p> <p>Note: aux phase errors per-post/user are not re-raised (swallowed in <code>AuxiliaryIngestors</code>), so they don't abort the run.</p> <p><code>close()</code>: Disposes the SQLAlchemy engine (closes all DB connections). Called in <code>cli.py</code>'s <code>finally</code> block.</p>"},{"location":"architecture/#dependency-injection-summary","title":"Dependency injection summary","text":"<p>All dependencies flow from <code>IngestionOrchestrator.__init__</code> downward \u2014 no singleton globals, no module-level state (except <code>asyncio.Lock</code> inside <code>PerKeyRateLimiter</code>, which is instance-scoped):</p> <pre><code>Settings\n  \u2514\u2500\u2500 IngestionOrchestrator\n        \u251c\u2500\u2500 build_engine \u2192 AsyncEngine\n        \u2502     \u2514\u2500\u2500 build_session_maker \u2192 async_sessionmaker\n        \u2502           \u2514\u2500\u2500 IngestionRepository\n        \u251c\u2500\u2500 PerKeyRateLimiter\n        \u2502     \u2514\u2500\u2500 SorsaClient\n        \u251c\u2500\u2500 SearchIngestor(repo, client)\n        \u2514\u2500\u2500 AuxiliaryIngestors(repo, client)\n</code></pre>"},{"location":"architecture/#concurrency-model","title":"Concurrency model","text":"<ul> <li>Phase 1 (search) is concurrent \u2014 <code>asyncio.gather</code> runs all slice coroutines in the same event loop, bounded by a semaphore.</li> <li>Phases 2\u20134 (comments, user-tweets, scores) are sequential \u2014 each phase iterates its input set with a plain <code>for</code> loop and <code>await</code>.</li> <li>All I/O (HTTP and DB) is async throughout \u2014 no blocking calls on the event loop thread.</li> <li>The rate limiter uses a single <code>asyncio.Lock</code> to serialize timestamp deque updates; it does not block the event loop while sleeping (it <code>await asyncio.sleep()</code>s).</li> </ul>"},{"location":"data-model/","title":"Data Model","text":"<p>This page documents the full CockroachDB schema used by the ingestion pipeline. All DDL lives in <code>ddl/ddl_mindshare_ingestion.sql</code>.</p>"},{"location":"data-model/#schemas","title":"Schemas","text":"<p>Two schemas are created:</p> Schema Purpose <code>mindshare</code> Normalized production data \u2014 posts, users, run tracking, checkpoints <code>raw_data</code> Raw JSONB API payloads for debugging and replay <pre><code>CREATE SCHEMA IF NOT EXISTS mindshare;\nCREATE SCHEMA IF NOT EXISTS raw_data;\n</code></pre>"},{"location":"data-model/#tables","title":"Tables","text":""},{"location":"data-model/#mindsharemindshare_user","title":"<code>mindshare.mindshare_user</code>","text":"<p>Stores one row per X/Twitter user, updated with their latest profile and influence score.</p> <pre><code>CREATE TABLE IF NOT EXISTS mindshare.mindshare_user (\n    x_id                  INT8          NOT NULL PRIMARY KEY,\n    x_username            STRING(255)   NOT NULL,\n    display_name          STRING(255)   NOT NULL,\n    score                 DECIMAL(10,2) NOT NULL,\n    avatar_url            STRING(1000)  NOT NULL,\n    adjustment_config     JSONB         NOT NULL,\n    followers_count       INT4          NOT NULL,\n    verified              BOOL          NOT NULL DEFAULT false,\n    created_at            TIMESTAMPTZ   NOT NULL DEFAULT current_timestamp(),\n    updated_at            TIMESTAMPTZ   NULL     DEFAULT current_timestamp(),\n    last_score_fetched_at TIMESTAMPTZ   NULL     DEFAULT current_timestamp()\n);\n\nCREATE INDEX IF NOT EXISTS idx_mindshare_user_x_username\n    ON mindshare.mindshare_user (x_username);\n</code></pre> <p>Column notes:</p> Column Notes <code>x_id</code> The numeric X/Twitter user ID (INT8). Primary key. <code>x_username</code> Twitter handle without <code>@</code>. <code>score</code> Influence score from <code>/score-id/{x_id}</code>. Stored as <code>DECIMAL(10,2)</code>. <code>adjustment_config</code> Initialized to <code>{\"default\": 1}</code> on insert. Not written by the ingestion pipeline beyond that default. <code>verified</code> Inserted as <code>false</code>; not populated from API data in current implementation. <code>last_score_fetched_at</code> Updated every time <code>upsert_user_score</code> runs for this user. <code>updated_at</code> Updated on every upsert conflict. <p>Populated by: <code>IngestionRepository.upsert_user_score()</code>, called from <code>AuxiliaryIngestors.ingest_scores()</code>.</p>"},{"location":"data-model/#mindsharemindshare_post","title":"<code>mindshare.mindshare_post</code>","text":"<p>The core normalized post table. One row per tweet, shared across all projects.</p> <pre><code>CREATE TABLE IF NOT EXISTS mindshare.mindshare_post (\n    -- Core IDs\n    post_id               INT8        NOT NULL PRIMARY KEY,\n    user_x_id             INT8        NOT NULL,\n\n    -- Source flags (not populated by ingestion pipeline)\n    source_m              BOOL        NOT NULL DEFAULT false,\n    source_n              BOOL        NOT NULL DEFAULT false,\n    source_u              BOOL        NOT NULL DEFAULT false,\n\n    -- Cross-project keyword set\n    project_keywords      TEXT[]      NULL,\n\n    -- Content\n    full_text             TEXT        NOT NULL,\n\n    -- Thread relationships\n    retweeted_post_id     INT8        NULL,\n    replied_post_id       INT8        NULL,\n    quoted_post_id        INT8        NULL,\n    root_post_id          INT8        NULL,\n\n    -- Computed type flags (generated columns, stored)\n    is_retweet  BOOL GENERATED ALWAYS AS (retweeted_post_id IS NOT NULL) STORED NOT NULL,\n    is_reply    BOOL GENERATED ALWAYS AS (replied_post_id   IS NOT NULL) STORED NOT NULL,\n    is_quote    BOOL GENERATED ALWAYS AS (quoted_post_id    IS NOT NULL) STORED NOT NULL,\n    is_post     BOOL GENERATED ALWAYS AS (\n                    retweeted_post_id IS NULL\n                    AND replied_post_id IS NULL\n                    AND quoted_post_id  IS NULL\n                ) STORED NOT NULL,\n\n    -- Engagement metrics\n    view_count            INT4        NOT NULL DEFAULT 0,\n    reply_count           INT4        NOT NULL DEFAULT 0,\n    retweet_count         INT4        NOT NULL DEFAULT 0,\n    quote_count           INT4        NOT NULL DEFAULT 0,\n    favorite_count        INT4        NOT NULL DEFAULT 0,\n\n    -- Raw entities (hashtags, mentions, URLs, etc.)\n    entities              JSONB       NULL,\n\n    -- Timestamps\n    post_created_at       TIMESTAMPTZ NOT NULL,\n    created_at            TIMESTAMPTZ NOT NULL DEFAULT current_timestamp(),\n    updated_at            TIMESTAMPTZ NULL     DEFAULT current_timestamp(),\n\n    -- Ingestion tracking\n    last_ingested_run_id  UUID        NULL,\n    last_seen_at          TIMESTAMPTZ NULL     DEFAULT current_timestamp()\n);\n</code></pre> <p>Indexes:</p> <pre><code>CREATE INDEX IF NOT EXISTS idx_mindshare_post_created_at\n    ON mindshare.mindshare_post (post_created_at DESC);\n\nCREATE INDEX IF NOT EXISTS idx_mindshare_post_user_x_id\n    ON mindshare.mindshare_post (user_x_id);\n\nCREATE INVERTED INDEX IF NOT EXISTS idx_mindshare_post_project_keywords\n    ON mindshare.mindshare_post (project_keywords);\n</code></pre> <p>The inverted index on <code>project_keywords</code> enables efficient array containment queries such as <code>WHERE 'Acurast' = ANY(project_keywords)</code>.</p> <p>Generated columns:</p> <p><code>is_retweet</code>, <code>is_reply</code>, <code>is_quote</code>, <code>is_post</code> are <code>GENERATED ALWAYS AS ... STORED</code> columns \u2014 CockroachDB computes and stores them automatically. They can be used in filters and indexes without extra application logic.</p> <p>Column-to-API-field mapping (how <code>upsert_mindshare_post</code> populates this table):</p> DB column Extracted from raw payload <code>post_id</code> <code>payload[\"id\"]</code> cast to INT8 <code>user_x_id</code> <code>payload[\"user\"][\"id\"]</code> cast to INT8 <code>project_keywords</code> <code>ARRAY[project_keyword]</code> \u2014 merged on conflict <code>full_text</code> <code>payload[\"full_text\"]</code> <code>retweeted_post_id</code> <code>payload[\"retweeted_status\"][\"id\"]</code> (NULLIF empty string) <code>replied_post_id</code> <code>payload[\"in_reply_to_tweet_id\"]</code> (NULLIF empty string) <code>quoted_post_id</code> <code>payload[\"quoted_status\"][\"id\"]</code> (NULLIF empty string) <code>root_post_id</code> <code>payload[\"conversation_id_str\"]</code> (NULLIF empty string) <code>view_count</code> <code>payload[\"view_count\"]</code> <code>reply_count</code> <code>payload[\"reply_count\"]</code> <code>retweet_count</code> <code>payload[\"retweet_count\"]</code> <code>quote_count</code> <code>payload[\"quote_count\"]</code> <code>favorite_count</code> <code>payload[\"likes_count\"]</code> <code>entities</code> <code>payload[\"entities\"]</code> as JSONB <code>post_created_at</code> <code>payload[\"created_at\"]</code> cast to TIMESTAMPTZ <code>last_ingested_run_id</code> <code>run_id</code> (the current run UUID) <p>Populated by: <code>IngestionRepository.upsert_mindshare_post()</code>, called for every tweet in search, comments, and user-tweets phases.</p>"},{"location":"data-model/#raw_dataraw_post_ingestion","title":"<code>raw_data.raw_post_ingestion</code>","text":"<p>Debug and replay store. Preserves the last raw API payload for every <code>(post_id, endpoint)</code> pair.</p> <pre><code>CREATE TABLE IF NOT EXISTS raw_data.raw_post_ingestion (\n    run_id          UUID        NOT NULL,\n    project_keyword TEXT        NOT NULL,\n    endpoint        TEXT        NOT NULL,\n    post_id         TEXT        NOT NULL,\n    raw_json        JSONB       NOT NULL,\n    fetched_at      TIMESTAMPTZ NOT NULL DEFAULT current_timestamp(),\n    PRIMARY KEY (post_id, endpoint)\n);\n\nCREATE INDEX IF NOT EXISTS idx_raw_post_ingestion_run\n    ON raw_data.raw_post_ingestion (run_id, endpoint, fetched_at DESC);\n</code></pre> <p>Design notes: - Primary key <code>(post_id, endpoint)</code> \u2014 a post can appear from multiple endpoints (e.g., once from <code>/search-tweets</code> and once from <code>/comments</code>). Each endpoint gets its own row. - <code>post_id</code> is stored as <code>TEXT</code> (not INT8) to accommodate any format returned by the API. - On conflict, <code>raw_json</code>, <code>fetched_at</code>, <code>project_keyword</code>, and <code>run_id</code> are overwritten with the most recent fetch.</p> <p>Populated by: <code>IngestionRepository.upsert_post_raw()</code>, called for every tweet in search, comments, and user-tweets phases, before the normalized upsert.</p>"},{"location":"data-model/#mindshareingestion_run","title":"<code>mindshare.ingestion_run</code>","text":"<p>One row per pipeline execution. Tracks run lifecycle.</p> <pre><code>CREATE TABLE IF NOT EXISTS mindshare.ingestion_run (\n    run_id          UUID        PRIMARY KEY,\n    project_keyword TEXT        NOT NULL,\n    since_ts        TIMESTAMPTZ NOT NULL,\n    until_ts        TIMESTAMPTZ NOT NULL,\n    run_status      TEXT        NOT NULL,\n    error_summary   TEXT        NULL,\n    started_at      TIMESTAMPTZ NOT NULL DEFAULT current_timestamp(),\n    finished_at     TIMESTAMPTZ NULL\n);\n\nCREATE INDEX IF NOT EXISTS idx_ingestion_run_status\n    ON mindshare.ingestion_run (run_status, started_at DESC);\n</code></pre> <p>Lifecycle states for <code>run_status</code>:</p> Value Set when <code>'running'</code> On <code>create_run()</code> \u2014 immediately at pipeline start <code>'completed'</code> On <code>mark_run_finished(status='completed')</code> \u2014 after all phases succeed <code>'failed'</code> On <code>mark_run_finished(status='failed', error=...)</code> \u2014 if any phase raises an uncaught exception <p>Populated by: <code>IngestionRepository.create_run()</code> and <code>IngestionRepository.mark_run_finished()</code>.</p>"},{"location":"data-model/#mindshareingestion_window_checkpoint","title":"<code>mindshare.ingestion_window_checkpoint</code>","text":"<p>Missing from DDL file</p> <p>This table is written to by <code>IngestionRepository.upsert_window_checkpoint()</code> but is not defined in <code>ddl/ddl_mindshare_ingestion.sql</code>. You must create it manually.</p> <p>Recommended DDL:</p> <pre><code>CREATE TABLE IF NOT EXISTS mindshare.ingestion_window_checkpoint (\n    run_id          UUID        NOT NULL,\n    project_keyword TEXT        NOT NULL,\n    window_id       TEXT        NOT NULL,\n    endpoint        TEXT        NOT NULL,\n    api_key_alias   TEXT        NOT NULL,\n    next_cursor     TEXT        NULL,\n    status          TEXT        NOT NULL,\n    error_message   TEXT        NULL,\n    updated_at      TIMESTAMPTZ NOT NULL DEFAULT current_timestamp(),\n    PRIMARY KEY (run_id, project_keyword, window_id, endpoint)\n);\n\nCREATE INDEX IF NOT EXISTS idx_ingestion_window_checkpoint_status\n    ON mindshare.ingestion_window_checkpoint (status, updated_at DESC);\n</code></pre> <p>Column notes:</p> Column Description <code>run_id</code> Links to <code>mindshare.ingestion_run.run_id</code> <code>project_keyword</code> The keyword being ingested <code>window_id</code> Identifies the specific paginated window \u2014 format varies by phase (see below) <code>endpoint</code> API endpoint path: <code>/search-tweets</code>, <code>/comments</code>, <code>/user-tweets</code>, <code>/score-id</code> <code>api_key_alias</code> Which key alias (<code>key_1</code>, etc.) was used for this window <code>next_cursor</code> The cursor value to resume pagination from; <code>NULL</code> when the window is complete <code>status</code> <code>'running'</code>, <code>'completed'</code>, or <code>'failed'</code> <code>error_message</code> Exception message on failure; <code>NULL</code> otherwise <p><code>window_id</code> naming conventions:</p> Phase Format Example Search slice <code>slice_{slice_id}</code> <code>slice_3</code> Comments per post <code>comments_{post_id}</code> <code>comments_1234567890</code> User timeline <code>user_{user_id}</code> <code>user_9876543210</code> Score fetch <code>score_{user_id}</code> <code>score_9876543210</code>"},{"location":"data-model/#upsert-conflict-behavior-summary","title":"Upsert conflict behavior summary","text":"<pre><code>flowchart LR\n    incoming[\"Incoming tweet\\n(post_id=123, keyword=B)\"]\n    check{post_id exists\\nin mindshare_post?}\n    insert[\"INSERT new row\\nproject_keywords = ['B']\"]\n    merge[\"ON CONFLICT DO UPDATE:\\n- keywords merged: ['A','B']\\n- engagement counts refreshed\\n- full_text preserved if non-empty\\n- relationship IDs coalesced\\n- last_seen_at = now()\"]\n    result[\"Single row\\npost_id=123\\nproject_keywords=['A','B']\"]\n\n    incoming --&gt; check\n    check --&gt;|No| insert --&gt; result\n    check --&gt;|Yes| merge --&gt; result</code></pre>"},{"location":"data-model/#mindshare_post-conflict-rules-full-detail","title":"<code>mindshare_post</code> conflict rules (full detail)","text":"Column On conflict behavior <code>project_keywords</code> <code>array_agg(DISTINCT k)</code> over <code>existing \\|\\| incoming</code> \u2014 deduplicates, set-union semantics <code>full_text</code> Replace only if incoming is non-empty; keep existing if incoming is empty string <code>user_x_id</code> Always overwritten with incoming value <code>retweeted_post_id</code> <code>COALESCE(incoming, existing)</code> \u2014 keeps first non-null <code>replied_post_id</code> <code>COALESCE(incoming, existing)</code> <code>quoted_post_id</code> <code>COALESCE(incoming, existing)</code> <code>root_post_id</code> <code>COALESCE(incoming, existing)</code> <code>view_count</code> Always overwritten <code>reply_count</code> Always overwritten <code>retweet_count</code> Always overwritten <code>quote_count</code> Always overwritten <code>favorite_count</code> Always overwritten <code>entities</code> <code>COALESCE(incoming, existing)</code> <code>last_ingested_run_id</code> Always overwritten <code>last_seen_at</code> Always set to <code>now()</code>"},{"location":"data-model/#mindshare_user-conflict-rules","title":"<code>mindshare_user</code> conflict rules","text":"Column On conflict behavior <code>x_username</code> <code>COALESCE(NULLIF(incoming, ''), existing)</code> \u2014 keeps existing if incoming is empty <code>display_name</code> Same pattern <code>score</code> <code>COALESCE(incoming, existing)</code> <code>avatar_url</code> <code>COALESCE(NULLIF(incoming, ''), existing)</code> <code>followers_count</code> <code>COALESCE(incoming, existing)</code> <code>last_score_fetched_at</code> Always set to <code>now()</code> <code>updated_at</code> Always set to <code>now()</code>"},{"location":"data-model/#raw_post_ingestion-conflict-rules","title":"<code>raw_post_ingestion</code> conflict rules","text":"<p>All mutable fields (<code>raw_json</code>, <code>fetched_at</code>, <code>project_keyword</code>, <code>run_id</code>) are always overwritten \u2014 last write wins.</p>"},{"location":"data-model/#ingestion_window_checkpoint-conflict-rules","title":"<code>ingestion_window_checkpoint</code> conflict rules","text":"<p><code>api_key_alias</code>, <code>next_cursor</code>, <code>status</code>, <code>error_message</code>, <code>updated_at</code> are always overwritten \u2014 the latest page fetch state replaces the previous.</p>"},{"location":"pipeline-behavior/","title":"Pipeline Behavior","text":"<p>This page documents the exact execution flow of the ingestion pipeline, phase by phase, from entry point to database commit.</p>"},{"location":"pipeline-behavior/#full-execution-sequence","title":"Full execution sequence","text":"<pre><code>sequenceDiagram\n    participant CLI as cli.py\n    participant Orch as IngestionOrchestrator\n    participant Repo as IngestionRepository\n    participant DB as CockroachDB\n    participant Search as SearchIngestor\n    participant Aux as AuxiliaryIngestors\n    participant API as Sorsa v3 API\n\n    CLI-&gt;&gt;Orch: run_project_ingestion(keyword)\n    Orch-&gt;&gt;Repo: create_run(keyword, since, until)\n    Repo-&gt;&gt;DB: INSERT ingestion_run (status=running)\n    DB--&gt;&gt;Repo: run_id (UUID)\n    Repo--&gt;&gt;Orch: run_id\n\n    Orch-&gt;&gt;Search: ingest_72h(run_id, keyword, keys, ...)\n    Note over Search: Builds N time slices, launches N async tasks\n\n    par Concurrent slice workers (bounded by semaphore)\n        Search-&gt;&gt;API: POST /search-tweets (slice_1, page_1)\n        API--&gt;&gt;Search: {tweets: [...], next_cursor: \"abc\"}\n        Search-&gt;&gt;Repo: upsert_post_raw \u00d7 len(tweets)\n        Search-&gt;&gt;Repo: upsert_mindshare_post \u00d7 len(tweets)\n        Search-&gt;&gt;Repo: upsert_window_checkpoint (status=running, cursor=abc)\n        Search-&gt;&gt;API: POST /search-tweets (slice_1, page_2, cursor=abc)\n        API--&gt;&gt;Search: {tweets: [...], next_cursor: null}\n        Search-&gt;&gt;Repo: upsert_post_raw \u00d7 len(tweets)\n        Search-&gt;&gt;Repo: upsert_mindshare_post \u00d7 len(tweets)\n        Search-&gt;&gt;Repo: upsert_window_checkpoint (status=completed)\n    end\n\n    Search--&gt;&gt;Orch: (post_ids: set, user_ids: set)\n\n    loop For each post_id in post_ids\n        Orch-&gt;&gt;Aux: ingest_comments_for_posts(post_id)\n        Aux-&gt;&gt;API: POST /comments (post_id, page_1)\n        API--&gt;&gt;Aux: {tweets: [...], next_cursor: ...}\n        Aux-&gt;&gt;Repo: upsert_post_raw + upsert_mindshare_post\n        Aux-&gt;&gt;Repo: upsert_window_checkpoint\n        Note over Aux: Paginate until next_cursor is null\n    end\n\n    loop For each user_id in user_ids\n        Orch-&gt;&gt;Aux: ingest_user_tweets(user_id)\n        Aux-&gt;&gt;API: POST /user-tweets (user_id, page_1)\n        API--&gt;&gt;Aux: {tweets: [...], next_cursor: ...}\n        Aux-&gt;&gt;Repo: upsert_post_raw + upsert_mindshare_post\n        Aux-&gt;&gt;Repo: upsert_window_checkpoint\n        Note over Aux: Paginate until next_cursor is null\n    end\n\n    loop For each user_id in user_ids\n        Orch-&gt;&gt;Aux: ingest_scores(user_id)\n        Aux-&gt;&gt;API: GET /score-id/{user_id}\n        API--&gt;&gt;Aux: {score, username, display_name, ...}\n        Aux-&gt;&gt;Repo: upsert_user_score\n        Aux-&gt;&gt;Repo: upsert_window_checkpoint (status=completed)\n    end\n\n    Orch-&gt;&gt;Repo: mark_run_finished(run_id, \"completed\")\n    Repo-&gt;&gt;DB: UPDATE ingestion_run SET run_status='completed', finished_at=now()</code></pre>"},{"location":"pipeline-behavior/#phase-1-search-search-tweets","title":"Phase 1 \u2014 Search (<code>/search-tweets</code>)","text":""},{"location":"pipeline-behavior/#time-window-setup","title":"Time window setup","text":"<p>Every run computes a fresh 72-hour window anchored to the moment of execution:</p> <pre><code>until = datetime.now(timezone.utc)\nsince = until - timedelta(hours=72)\n</code></pre> <p>Both <code>SearchIngestor.ingest_72h</code> and <code>IngestionOrchestrator.run_project_ingestion</code> compute this window independently \u2014 they are consistent because both use <code>now()</code> at call time and 72h is the fixed offset.</p>"},{"location":"pipeline-behavior/#time-slicing","title":"Time slicing","text":"<p><code>build_time_slices(since, until, slice_count, keys)</code> divides the 72-hour window into <code>SEARCH_SLICE_COUNT</code> equal-width sub-windows:</p> <pre><code>step = total_seconds / slice_count\nfor i in range(slice_count):\n    start = since + timedelta(seconds=i * step)\n    end   = since + timedelta(seconds=(i + 1) * step)  # last slice always ends at `until`\n    key_alias = keys[i % len(keys)].alias\n</code></pre> <p>Each <code>TimeSlice</code> carries its <code>slice_id</code> (1-based), <code>since</code>, <code>until</code>, and the assigned <code>api_key_alias</code>.</p> <p>With the default <code>SEARCH_SLICE_COUNT=20</code> over 72 hours, each slice covers exactly 3.6 hours (216 minutes).</p>"},{"location":"pipeline-behavior/#concurrency","title":"Concurrency","text":"<pre><code>effective_concurrency = max(1, min(slice_count, max_concurrency, per_key_rps))\nsem = asyncio.Semaphore(effective_concurrency)\n</code></pre> <p>All <code>slice_count</code> coroutines are created immediately via <code>asyncio.gather(*tasks)</code>. Each coroutine acquires the semaphore at the start of <code>_ingest_slice</code> \u2014 so at most <code>effective_concurrency</code> slices run at the same time. Completed slices release the semaphore, allowing waiting slices to start.</p>"},{"location":"pipeline-behavior/#per-slice-pagination-loop","title":"Per-slice pagination loop","text":"<p>For each slice, <code>_ingest_slice</code> runs:</p> <pre><code>cursor = None\nwindow_id = f\"slice_{slice_id}\"\nloop:\n  data = POST /search-tweets (query, since, until, cursor, order)\n  for tweet in data[\"tweets\"]:\n    upsert_post_raw(endpoint=\"/search-tweets\", payload=tweet)\n    upsert_mindshare_post(payload=tweet)\n    collect post_id, user_id into local sets\n  cursor = data.get(\"next_cursor\")\n  if cursor:\n    upsert_window_checkpoint(status=\"running\", next_cursor=cursor)\n    continue\n  else:\n    upsert_window_checkpoint(status=\"completed\", next_cursor=None)\n    return (post_ids, user_ids)\n</code></pre> <p>On exception at any point: <pre><code>upsert_window_checkpoint(status=\"failed\", error_message=str(exc))\nraise  \u2190 propagates to asyncio.gather, which fails the whole search phase\n</code></pre></p>"},{"location":"pipeline-behavior/#search-query-format","title":"Search query format","text":"<p>The query sent to the API is:</p> <pre><code>{project_keyword} since:{YYYY-MM-DD_HH:MM:SS_UTC} until:{YYYY-MM-DD_HH:MM:SS_UTC}\n</code></pre> <p>The <code>to_sorsa_date(dt)</code> helper in <code>sorsa_client.py</code> formats the datetime into this string.</p>"},{"location":"pipeline-behavior/#output","title":"Output","text":"<p><code>ingest_72h</code> returns <code>(all_post_ids: set[str], all_user_ids: set[str])</code> \u2014 the union of all IDs across all slices, deduplicated. These sets drive Phases 2\u20134.</p>"},{"location":"pipeline-behavior/#phase-2-comments-comments","title":"Phase 2 \u2014 Comments (<code>/comments</code>)","text":"<p><code>AuxiliaryIngestors.ingest_comments_for_posts(posts, key, ...)</code> iterates over every <code>post_id</code> discovered in Phase 1.</p> <p>For each post:</p> <pre><code>window_id = f\"comments_{post_id}\"\ncursor = None\nloop:\n  data = POST /comments (tweet_link=post_id, next_cursor=cursor)\n  for tweet in data[\"tweets\"]:\n    upsert_post_raw(endpoint=\"/comments\", payload=tweet)\n    upsert_mindshare_post(payload=tweet, project_keyword=keyword)\n  cursor = data.get(\"next_cursor\")\n  upsert_window_checkpoint(status=\"running\"|\"completed\", next_cursor=cursor)\n  if not cursor: break\n</code></pre> <p>On exception: <pre><code>upsert_window_checkpoint(status=\"failed\", error_message=str(exc))\nbreak  \u2190 moves to next post (does not propagate)\n</code></pre></p> <p>Important: exceptions are caught and swallowed at the per-post level. A failure on one post's comments does not abort Phase 2 or the run. The failure is recorded in the checkpoint table.</p> <p>Comments discovered here are inserted into <code>mindshare_post</code> under the same <code>project_keyword</code> as the search. This means a comment thread post can be associated with the keyword even if it wasn't returned by the search query directly.</p>"},{"location":"pipeline-behavior/#phase-3-user-timelines-user-tweets","title":"Phase 3 \u2014 User timelines (<code>/user-tweets</code>)","text":"<p><code>AuxiliaryIngestors.ingest_user_tweets(user_ids, key, ...)</code> iterates over every <code>user_id</code> discovered in Phase 1.</p> <p>For each user:</p> <pre><code>window_id = f\"user_{user_id}\"\ncursor = None\nloop:\n  data = POST /user-tweets (user_id=user_id, next_cursor=cursor)\n  for tweet in data[\"tweets\"]:\n    upsert_post_raw(endpoint=\"/user-tweets\", payload=tweet)\n    upsert_mindshare_post(payload=tweet, project_keyword=keyword)\n  cursor = data.get(\"next_cursor\")\n  upsert_window_checkpoint(status=\"running\"|\"completed\", next_cursor=cursor)\n  if not cursor: break\n</code></pre> <p>On exception: checkpoint written as <code>failed</code>, loop breaks to next user.</p> <p>User timeline tweets are also upserted into <code>mindshare_post</code> under the project keyword. This may associate unrelated tweets from a user's timeline with the project \u2014 this is intentional for mindshare tracking.</p>"},{"location":"pipeline-behavior/#phase-4-user-scores-score-idx_id","title":"Phase 4 \u2014 User scores (<code>/score-id/{x_id}</code>)","text":"<p><code>AuxiliaryIngestors.ingest_scores(user_ids, key, ...)</code> iterates over every <code>user_id</code> discovered in Phase 1.</p> <p>For each user:</p> <pre><code>window_id = f\"score_{user_id}\"\ndata = GET /score-id/{user_id}\nupsert_user_score(\n    x_id=user_id,\n    username=data[\"username\"],\n    display_name=data[\"display_name\"],\n    avatar_url=data[\"profile_image_url\"],\n    followers_count=data[\"followers_count\"],\n    score=data[\"score\"]\n)\nupsert_window_checkpoint(status=\"completed\")\n</code></pre> <p>On exception: checkpoint written as <code>failed</code>, continues to next user. Scores are not paginated \u2014 one HTTP call per user.</p>"},{"location":"pipeline-behavior/#retry-and-rate-limit-strategy","title":"Retry and rate-limit strategy","text":"<p>All HTTP calls flow through <code>SorsaClient._request()</code>.</p>"},{"location":"pipeline-behavior/#rate-limiting","title":"Rate limiting","text":"<p>The <code>PerKeyRateLimiter</code> enforces a flat limit of <code>SORSA_PER_KEY_RPS</code> requests per second per API key alias (default: 20 req/s):</p> <ul> <li>Maintains a <code>deque[float]</code> of request timestamps per alias.</li> <li>Every call to <code>acquire(alias)</code> checks: if fewer than <code>rps</code> requests were made in the last 1 second, record the timestamp and allow the request. Otherwise, sleep until the oldest tracked request is more than 1 second old, then retry.</li> <li>The lock is released before sleeping so other coroutines are not blocked.</li> <li><code>acquire()</code> is called before every retry attempt, not just the first \u2014 the per-second cap applies uniformly regardless of attempt count.</li> </ul>"},{"location":"pipeline-behavior/#retry-logic","title":"Retry logic","text":"Error type Retry condition Sleep before retry HTTP 429 If <code>attempt &lt; max_retries</code> <code>retry_429_sleep_seconds</code> (default: 1.0s) HTTP 5xx If <code>attempt &lt; max_retries</code> <code>retry_5xx_sleep_seconds</code> (default: 2.0s) <code>aiohttp.ClientError</code> / <code>asyncio.TimeoutError</code> If <code>attempt &lt; max_retries</code> <code>retry_5xx_sleep_seconds</code> HTTP 429 on final attempt \u2014 Raises <code>SorsaRateLimitError</code> Any other error after retries \u2014 Raises <code>SorsaClientError</code> <p>The client timeout is set to 60 seconds (<code>aiohttp.ClientTimeout(total=60)</code>) in the orchestrator.</p>"},{"location":"pipeline-behavior/#error-propagation-summary","title":"Error propagation summary","text":"Error source Behavior Search slice (any page) Re-raised \u2192 propagates through <code>asyncio.gather</code> \u2192 aborts search phase \u2192 marks run <code>failed</code> Comments per post Swallowed \u2192 next post starts; run continues User timeline per user Swallowed \u2192 next user starts; run continues Score per user Swallowed \u2192 next user starts; run continues"},{"location":"pipeline-behavior/#checkpoint-lifecycle","title":"Checkpoint lifecycle","text":"<p>Checkpoints allow visibility into the granular state of every paginated window. The <code>status</code> transitions are:</p> <pre><code>(not yet started) \u2192 running \u2192 completed\n                            \u2198 failed\n</code></pre> <p>A <code>running</code> checkpoint with a non-null <code>next_cursor</code> represents an interrupted or in-progress window. The cursor value needed to resume pagination is preserved.</p> <p>Resume is not yet wired</p> <p>Checkpoint writes are implemented, but the resume reader (which would look up the last <code>running</code> cursor and skip already-completed slices) is not yet implemented. Each CLI invocation starts a fresh <code>ingestion_run</code> and re-fetches from the beginning of the 72-hour window.</p>"},{"location":"pipeline-behavior/#data-flow-per-tweet","title":"Data flow per tweet","text":"<p>Every tweet API response passes through this pipeline:</p> <pre><code>flowchart TD\n    api[\"Raw API response\\n{id, full_text, user, entities, ...}\"]\n    raw[\"raw_data.raw_post_ingestion\\nstores JSONB payload\\nkeyed by (post_id, endpoint)\"]\n    norm[\"mindshare.mindshare_post\\nnormalized columns\\nproject_keywords merged\"]\n    skip{post_id and\\nuser_x_id and\\ncreated_at present?}\n\n    api --&gt; skip\n    skip --&gt;|Yes| raw\n    skip --&gt;|Yes| norm\n    skip --&gt;|No| drop[\"Silently skipped\\n(no DB write)\"]</code></pre> <p>Both writes happen for every tweet in every phase (search, comments, user-tweets). The raw write happens first, then the normalized upsert.</p>"},{"location":"pipeline-behavior/#run-outcome-states","title":"Run outcome states","text":"Scenario <code>ingestion_run.run_status</code> Notes All phases complete <code>completed</code> Normal exit Any slice in Phase 1 fails <code>failed</code> <code>error_summary</code> contains exception string All of Phase 1 succeeds but a comment/timeline/score call fails <code>completed</code> Per-item errors in aux phases are swallowed; run still completes Unexpected exception escapes the orchestrator <code>failed</code> Covers programming errors, DB connection loss, etc."},{"location":"quickstart/","title":"Quickstart","text":"<p>This page walks through everything needed to configure and run the ingestion pipeline from a clean checkout.</p>"},{"location":"quickstart/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.12 (see <code>.python-version</code> in the repo root \u2014 enforced by pyenv if present)</li> <li>CockroachDB instance reachable over a <code>postgresql+asyncpg://</code> SQLAlchemy URL</li> <li>A Sorsa v3 API key</li> </ul>"},{"location":"quickstart/#1-install-dependencies","title":"1. Install dependencies","text":"<p>From the repository root:</p> <pre><code>pip install -e .\n</code></pre> <p>Dependencies declared in <code>pyproject.toml</code>:</p> Package Purpose <code>aiohttp&gt;=3.12.0</code> Async HTTP client used by <code>SorsaClient</code> <code>asyncpg&gt;=0.30.0</code> Async PostgreSQL driver used by SQLAlchemy <code>python-dotenv&gt;=1.1.0</code> Loads <code>.env</code> file before <code>Settings</code> is instantiated <code>pydantic&gt;=2.11.0</code> Data validation for <code>Settings</code> and dataclasses <code>pydantic-settings&gt;=2.10.0</code> Env-var binding for <code>Settings</code> <code>sqlalchemy&gt;=2.0.0</code> Async engine and session management <p>Optional \u2014 for building these docs:</p> <pre><code>pip install -e \".[docs]\"\n</code></pre>"},{"location":"quickstart/#2-apply-the-database-schema","title":"2. Apply the database schema","text":"<p>Run the ingestion DDL against your CockroachDB cluster. The file is idempotent (<code>IF NOT EXISTS</code> guards on every object):</p> <pre><code>cockroach sql --url \"$COCKROACH_DATABASE_URL\" &lt; ddl/ddl_mindshare_ingestion.sql\n</code></pre> <p>Or paste the contents of <code>ddl/ddl_mindshare_ingestion.sql</code> directly into your preferred SQL client.</p> <p>Missing checkpoint table</p> <p><code>ingestion_window_checkpoint</code> is written to by the repository but is not defined in <code>ddl/ddl_mindshare_ingestion.sql</code>. You must create this table manually before running the pipeline. See the Data Model page for the full DDL.</p> <p>Do not edit or replace <code>Mindshare DDL/ddl mindshare.sql</code> \u2014 that file is intentionally preserved as the legacy reference copy.</p>"},{"location":"quickstart/#3-configure-environment-variables","title":"3. Configure environment variables","text":"<p>Copy the example file and fill in your values:</p> <pre><code>cp .env.example .env\n</code></pre>"},{"location":"quickstart/#required-variables","title":"Required variables","text":"Variable Description <code>COCKROACH_DATABASE_URL</code> SQLAlchemy async URL: <code>postgresql+asyncpg://USER:PASS@HOST:26257/DB?sslmode=require</code> <code>SORSA_API_KEY</code> Your Sorsa v3 API key"},{"location":"quickstart/#optional-tuning-variables","title":"Optional tuning variables","text":"Variable Default Description <code>SORSA_BASE_URL</code> <code>https://api.sorsa.io/v3</code> Sorsa API base URL <code>SORSA_PER_KEY_RPS</code> <code>20</code> Maximum requests per second for the API key <code>SEARCH_SLICE_COUNT</code> <code>20</code> How many time slices the 72-hour window is divided into <code>SEARCH_MAX_CONCURRENCY</code> <code>20</code> Upper bound on concurrent slice worker tasks <code>SEARCH_ORDER</code> <code>latest</code> Tweet sort order passed to <code>/search-tweets</code> <code>SORSA_MAX_RETRIES</code> <code>4</code> Maximum retry attempts per HTTP request <code>SORSA_RETRY_429_SLEEP_SECONDS</code> <code>1.0</code> Sleep duration between retries on HTTP 429 <code>SORSA_RETRY_5XX_SLEEP_SECONDS</code> <code>2.0</code> Sleep duration between retries on HTTP 5xx or network error"},{"location":"quickstart/#how-effective-concurrency-is-computed","title":"How effective concurrency is computed","text":"<p>The pipeline caps concurrent slice workers at:</p> <pre><code>effective_concurrency = min(SEARCH_SLICE_COUNT, SEARCH_MAX_CONCURRENCY, SORSA_PER_KEY_RPS)\n</code></pre> <p>This is enforced via a <code>asyncio.Semaphore</code> in <code>SearchIngestor.ingest_72h</code>. Examples:</p> <code>SEARCH_SLICE_COUNT</code> <code>SEARCH_MAX_CONCURRENCY</code> <code>SORSA_PER_KEY_RPS</code> Effective concurrency 10 20 20 10 20 20 20 20 20 5 20 5 20 20 8 8"},{"location":"quickstart/#4-run-the-pipeline","title":"4. Run the pipeline","text":"<pre><code>python main.py --project-keyword &lt;keyword&gt;\n</code></pre> <p>Example:</p> <pre><code>python main.py --project-keyword Acurast\n</code></pre> <p>On success, the CLI prints:</p> <pre><code>Ingestion completed. run_id=&lt;uuid&gt;\n</code></pre> <p>The <code>run_id</code> corresponds to a row in <code>mindshare.ingestion_run</code> and can be used to query checkpoints and raw data.</p>"},{"location":"quickstart/#5-verify-the-run","title":"5. Verify the run","text":"<p>Check the run record:</p> <pre><code>SELECT run_id, project_keyword, run_status, started_at, finished_at\nFROM mindshare.ingestion_run\nORDER BY started_at DESC\nLIMIT 5;\n</code></pre> <p>Check ingested posts:</p> <pre><code>SELECT COUNT(*), project_keywords\nFROM mindshare.mindshare_post\nGROUP BY project_keywords\nORDER BY COUNT(*) DESC;\n</code></pre>"},{"location":"quickstart/#how-env-loading-works","title":"How <code>.env</code> loading works","text":"<p><code>app/cli.py</code> calls <code>load_dotenv()</code> explicitly before instantiating <code>Settings</code>. This means:</p> <ul> <li>A <code>.env</code> file at the working directory will always be picked up.</li> <li>The <code>Settings</code> class (Pydantic settings) will then override with any actual environment variables that are already set, per standard Pydantic-settings precedence.</li> <li>The <code>extra = \"ignore\"</code> setting means unknown variables in <code>.env</code> are silently ignored.</li> </ul>"},{"location":"runbook/","title":"Operations Runbook","text":"<p>This page covers how to run, monitor, debug, and tune the ingestion pipeline.</p>"},{"location":"runbook/#running-the-pipeline","title":"Running the pipeline","text":""},{"location":"runbook/#local-run","title":"Local run","text":"<pre><code># From the repository root\npython main.py --project-keyword &lt;keyword&gt;\n</code></pre> <p>Example:</p> <pre><code>python main.py --project-keyword Acurast\n</code></pre> <p>On success:</p> <pre><code>Ingestion completed. run_id=3fa85f64-5717-4562-b3fc-2c963f66afa6\n</code></pre> <p>On failure, a Python traceback is printed and the process exits non-zero.</p>"},{"location":"runbook/#running-with-custom-tuning","title":"Running with custom tuning","text":"<p>Override any setting via environment variable:</p> <pre><code>SEARCH_SLICE_COUNT=10 SORSA_PER_KEY_RPS=10 python main.py --project-keyword Acurast\n</code></pre> <p>Or set them all in <code>.env</code> before running.</p>"},{"location":"runbook/#building-and-serving-the-docs-site","title":"Building and serving the docs site","text":"<p>From <code>ingestion-docs/</code>:</p> <pre><code># Live preview (watches for changes)\nmkdocs serve\n\n# Build static site to ingestion-docs/site/\nmkdocs build\n</code></pre>"},{"location":"runbook/#monitoring-a-run","title":"Monitoring a run","text":""},{"location":"runbook/#1-check-run-status","title":"1. Check run status","text":"<pre><code>SELECT run_id, project_keyword, run_status, started_at, finished_at,\n       finished_at - started_at AS duration\nFROM mindshare.ingestion_run\nORDER BY started_at DESC\nLIMIT 20;\n</code></pre> <p>Possible <code>run_status</code> values: <code>running</code>, <code>completed</code>, <code>failed</code>.</p> <p>A run stuck in <code>running</code> with an old <code>started_at</code> and no <code>finished_at</code> indicates the process was killed or crashed without updating the record.</p>"},{"location":"runbook/#2-check-all-checkpoints-for-a-run","title":"2. Check all checkpoints for a run","text":"<pre><code>SELECT window_id, endpoint, status, next_cursor, error_message, updated_at\nFROM mindshare.ingestion_window_checkpoint\nWHERE run_id = '&lt;run_id&gt;'\nORDER BY updated_at DESC;\n</code></pre>"},{"location":"runbook/#3-check-failed-windows","title":"3. Check failed windows","text":"<pre><code>SELECT run_id, project_keyword, window_id, endpoint, status, error_message, updated_at\nFROM mindshare.ingestion_window_checkpoint\nWHERE status = 'failed'\nORDER BY updated_at DESC\nLIMIT 50;\n</code></pre> <p>Failed windows in Phase 1 (<code>endpoint = '/search-tweets'</code>) indicate the run itself failed. Failed windows in aux phases indicate per-item failures that were silently swallowed.</p>"},{"location":"runbook/#4-count-ingested-posts-for-a-run","title":"4. Count ingested posts for a run","text":"<pre><code>SELECT COUNT(*)\nFROM raw_data.raw_post_ingestion\nWHERE run_id = '&lt;run_id&gt;';\n</code></pre>"},{"location":"runbook/#5-inspect-raw-payload-for-a-specific-post","title":"5. Inspect raw payload for a specific post","text":"<pre><code>SELECT run_id, project_keyword, endpoint, raw_json, fetched_at\nFROM raw_data.raw_post_ingestion\nWHERE post_id = '&lt;post_id&gt;'\nORDER BY fetched_at DESC;\n</code></pre>"},{"location":"runbook/#6-verify-project-keyword-merge-on-a-known-post","title":"6. Verify project keyword merge on a known post","text":"<p>If the same post was ingested by two different keyword runs, its <code>project_keywords</code> array should contain both keywords:</p> <pre><code>SELECT post_id, project_keywords, last_seen_at, last_ingested_run_id\nFROM mindshare.mindshare_post\nWHERE post_id = &lt;tweet_id_as_integer&gt;;\n</code></pre>"},{"location":"runbook/#7-count-posts-per-keyword","title":"7. Count posts per keyword","text":"<pre><code>SELECT unnest(project_keywords) AS keyword, COUNT(*) AS post_count\nFROM mindshare.mindshare_post\nGROUP BY keyword\nORDER BY post_count DESC;\n</code></pre>"},{"location":"runbook/#8-check-user-scores-populated","title":"8. Check user scores populated","text":"<pre><code>SELECT x_id, x_username, score, followers_count, last_score_fetched_at\nFROM mindshare.mindshare_user\nORDER BY last_score_fetched_at DESC\nLIMIT 50;\n</code></pre>"},{"location":"runbook/#diagnosing-failures","title":"Diagnosing failures","text":""},{"location":"runbook/#run-marked-failed","title":"Run marked <code>failed</code>","text":"<ol> <li>Query <code>error_summary</code> in <code>ingestion_run</code>:    <pre><code>SELECT error_summary FROM mindshare.ingestion_run WHERE run_id = '&lt;run_id&gt;';\n</code></pre></li> <li>Find the failing search slice checkpoint:    <pre><code>SELECT window_id, error_message\nFROM mindshare.ingestion_window_checkpoint\nWHERE run_id = '&lt;run_id&gt;' AND status = 'failed' AND endpoint = '/search-tweets';\n</code></pre></li> <li>The error message is the Python exception string from <code>SorsaClient</code> \u2014 typically a Sorsa API error response or a network timeout.</li> </ol>"},{"location":"runbook/#checkpoint-table-missing","title":"Checkpoint table missing","text":"<p>If you see errors like: <pre><code>relation \"mindshare.ingestion_window_checkpoint\" does not exist\n</code></pre></p> <p>Apply the DDL from the Data Model page.</p>"},{"location":"runbook/#rate-limit-errors-sorsaratelimiterror","title":"Rate limit errors (<code>SorsaRateLimitError</code>)","text":"<p>If the run fails with a <code>SorsaRateLimitError</code>, the Sorsa API returned HTTP 429 on all retry attempts. Solutions:</p> <ul> <li>Lower <code>SORSA_PER_KEY_RPS</code> to reduce the burst rate.</li> <li>Increase <code>SORSA_RETRY_429_SLEEP_SECONDS</code>.</li> <li>Lower <code>SEARCH_SLICE_COUNT</code> to reduce total concurrent requests.</li> </ul>"},{"location":"runbook/#db-connection-errors","title":"DB connection errors","text":"<ul> <li>Confirm <code>COCKROACH_DATABASE_URL</code> is correct.</li> <li>Verify the CockroachDB cluster is reachable from your host.</li> <li>Check that SSL mode is set correctly (<code>sslmode=require</code> or <code>sslmode=disable</code> for local dev).</li> <li>The engine uses <code>pool_pre_ping=True</code>, so stale connections are caught early.</li> </ul>"},{"location":"runbook/#posts-missing-after-a-run","title":"Posts missing after a run","text":"<p>Check the raw table first \u2014 if the post is there but not in <code>mindshare_post</code>, the normalized upsert likely skipped it due to missing fields. The <code>upsert_mindshare_post</code> method silently skips rows where <code>post_id</code>, <code>user_x_id</code>, or <code>created_at</code> are absent/empty:</p> <pre><code>-- Check if raw payload has the expected id field\nSELECT raw_json-&gt;&gt;'id', raw_json-&gt;&gt;'created_at', raw_json-&gt;'user'-&gt;&gt;'id'\nFROM raw_data.raw_post_ingestion\nWHERE post_id = '&lt;post_id&gt;';\n</code></pre>"},{"location":"runbook/#tuning-guidance","title":"Tuning guidance","text":""},{"location":"runbook/#increase-throughput","title":"Increase throughput","text":"Lever Effect Risk Increase <code>SEARCH_SLICE_COUNT</code> More parallel slice workers, smaller per-slice time window Diminishing returns above <code>SORSA_PER_KEY_RPS</code>; more connections to DB Increase <code>SEARCH_MAX_CONCURRENCY</code> Raises the ceiling on concurrent tasks No effect if already limited by <code>SEARCH_SLICE_COUNT</code> or <code>SORSA_PER_KEY_RPS</code> Increase <code>SORSA_PER_KEY_RPS</code> More requests per second to Sorsa Will trigger 429s if set above actual API quota <p>Effective concurrency is always: <pre><code>min(SEARCH_SLICE_COUNT, SEARCH_MAX_CONCURRENCY, SORSA_PER_KEY_RPS)\n</code></pre></p> <p>So all three levers must be raised together to increase effective concurrency.</p>"},{"location":"runbook/#reduce-rate-limit-pressure","title":"Reduce rate-limit pressure","text":"<ul> <li>Lower <code>SORSA_PER_KEY_RPS</code> to stay within Sorsa's quota.</li> <li>Increase <code>SORSA_RETRY_429_SLEEP_SECONDS</code> to back off more gently on 429s.</li> <li>Lower <code>SEARCH_SLICE_COUNT</code> to reduce the number of concurrent outstanding requests.</li> </ul>"},{"location":"runbook/#reduce-db-pressure","title":"Reduce DB pressure","text":"<ul> <li>The current implementation issues one DB call per tweet per phase (not batched). For high-volume keywords, this results in many small transactions. Future optimization: batch upserts.</li> </ul>"},{"location":"runbook/#known-limitations","title":"Known limitations","text":""},{"location":"runbook/#checkpoint-resume-not-wired","title":"Checkpoint resume not wired","text":"<p>Checkpoints are written after every page fetch. The cursor value is stored and could be used to resume interrupted windows. However, the resume reader is not implemented \u2014 every CLI run creates a brand new <code>ingestion_run</code> and re-fetches the full 72-hour window from scratch. If a run is interrupted halfway through, re-running it will re-fetch everything from the start (safely, due to upsert idempotency), not from the last successful checkpoint.</p> <p>Impact: no data loss, but duplicated API calls on re-runs.</p>"},{"location":"runbook/#single-api-key-only","title":"Single API key only","text":"<p><code>Settings.api_keys</code> always returns a single-element list (<code>[SORSA_API_KEY.strip()]</code>). The round-robin key assignment in <code>build_time_slices</code> and the <code>PerKeyRateLimiter</code> are already designed for multiple keys, but <code>config.py</code> does not parse a comma-separated key list. All slices and aux calls use <code>key_1</code>.</p>"},{"location":"runbook/#sequential-aux-phases","title":"Sequential aux phases","text":"<p>Phases 2, 3, and 4 iterate their input sets with a plain <code>for</code> loop \u2014 no concurrency within each phase. For large result sets (many posts or users), this can be slow. Future work: async task pool for aux phases mirroring the slice concurrency in Phase 1.</p>"},{"location":"runbook/#per-tweet-db-transactions","title":"Per-tweet DB transactions","text":"<p>Each <code>upsert_post_raw</code> and <code>upsert_mindshare_post</code> call opens, executes, commits, and closes its own session. For high-volume ingestion, bulk/batched upserts would significantly reduce DB round trips.</p>"},{"location":"runbook/#source_m-source_n-source_u-flags-not-populated","title":"<code>source_m / source_n / source_u</code> flags not populated","text":"<p><code>mindshare_post</code> has three source flag columns (<code>source_m</code>, <code>source_n</code>, <code>source_u</code>). These are not written by the ingestion pipeline and always default to <code>false</code>. Their intended semantics are not defined in this codebase.</p>"},{"location":"runbook/#ingestioncontext-dataclass-unused","title":"<code>IngestionContext</code> dataclass unused","text":"<p><code>app/models.py</code> defines <code>IngestionContext</code> (run_id, project_keyword, slice_id, endpoint, api_key_alias). It is not referenced anywhere in the current implementation. It was likely intended for structured context propagation to the repository layer.</p>"},{"location":"runbook/#re-running-after-failure","title":"Re-running after failure","text":"<p>Because all writes are idempotent upserts, re-running the same keyword is always safe:</p> <pre><code>python main.py --project-keyword Acurast\n</code></pre> <p>This creates a new <code>ingestion_run</code> row and re-fetches the 72-hour window. Existing posts in <code>mindshare_post</code> will have their engagement counts refreshed and their <code>project_keywords</code> left unchanged (the keyword is already in the array). The <code>raw_post_ingestion</code> table will have its <code>raw_json</code> updated to the latest payload.</p>"},{"location":"runbook/#recommended-next-steps","title":"Recommended next steps","text":"<ol> <li>Create <code>mindshare.ingestion_window_checkpoint</code> in <code>ddl/ddl_mindshare_ingestion.sql</code> (see Data Model).</li> <li>Implement checkpoint resume \u2014 on start, look up the last <code>running</code> or <code>failed</code> checkpoint for each slice/window and skip already-completed windows or resume from stored cursor.</li> <li>Add multi-key support \u2014 parse <code>SORSA_API_KEY</code> as a comma-separated list in <code>Settings.api_keys</code>.</li> <li>Batch DB writes \u2014 accumulate tweets per page and issue a single multi-row upsert instead of one per tweet.</li> <li>Add concurrency to aux phases \u2014 use an <code>asyncio.Semaphore</code>-bounded task pool similar to Phase 1's slice workers.</li> </ol>"}]}